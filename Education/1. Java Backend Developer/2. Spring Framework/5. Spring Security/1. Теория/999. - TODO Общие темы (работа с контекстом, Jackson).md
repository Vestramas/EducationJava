# 0. Как работать с SecurityContext/Holder, и Authencation

---
# 1. Jackson - конвертер данных
Рассказать про 
```java
@Configuration  
@EnableWebMvc  
public class WebConfig implements WebMvcConfigurer {  
    @Override  
    public void configureMessageConverters(List<HttpMessageConverter<?>> converters) {  
        ObjectMapper objectMapper = new ObjectMapper();  
  
        //Поддержка десериализации времени  
        objectMapper.registerModule(new JavaTimeModule());  
  
        // Устанавливаем стратегию именования  
        objectMapper.setPropertyNamingStrategy(PropertyNamingStrategies.SNAKE_CASE);  
  
  
        MappingJackson2HttpMessageConverter jsonConverter =  
                new MappingJackson2HttpMessageConverter(objectMapper);  
  
        converters.add(jsonConverter);  
    }  
}
```

Что тут, и зачем, и про вообще MappingJackson2HttpMessageConverter

ПОТОМ, когда напишу практическое руковдство про MVC, отсюда инфу можно будет перенести туда